extends ./layouts/narbutas

block main_content
  .page-title
    h2.page-title_h2= __('narbutas')
      | : 
      = page.title || page.name

  .c-cat-menu_inline.d-lg-none
    +categoriesMenu(narbutasMenu)

  +narbutasPagesListBlock(pages)

  if (page.htmlContent)
    #narbutas-html!= page.htmlContent